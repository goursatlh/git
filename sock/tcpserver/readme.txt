tcpsev:
	recv requestes from more than one client at the same 
	time,and send datas to client after the request.
tcpclis:
	send request to sev,
	recv data from sev.

requestes:
1, the server and the clients can be started in any hosts.
2, it permits to start mulitiple clients at the same time.

